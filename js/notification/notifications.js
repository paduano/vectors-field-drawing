var Notifications = Notifications || {};

Notifications.drawingCanvasMode = {
    MODE_CHANGED: "drawingCanvasMode.MODE_CHANGED"
};


Notifications.layers = {
    VISIBLE_LAYERS_CHANGED: "layers.VISIBLE_LAYERS_CHANGED"
};

Notifications.colors = {
    SELECTED_COLOR_CHANGED: "colors.SELECTED_COLOR_CHANGED"
};